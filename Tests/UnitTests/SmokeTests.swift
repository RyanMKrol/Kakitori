import XCTest

/// Placeholder proving the unit-test target runs; the first real task replaces it.
final class SmokeTests: XCTestCase {
    func testTargetRuns() {
        XCTAssertTrue(true)
    }
}
