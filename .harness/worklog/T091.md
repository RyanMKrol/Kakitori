# T091 — Rework Summary buttons to Free Study (top) and Back home (bottom)

## 2026-07-25 — done (by hand, off-loop)

Summary now shows a blue Free Study button on top (`start-free-study`) and "Back home" / "Back to
decks" below (`back-to-decks`). The old "Study another" button and its `onStudyAnother` path are
gone with no dangling references. Free Study picks the same deck back up in the mode just finished,
via `startFreeStudySession(deck:mode:)`.

Open caveat, NOT fixed here: the spec's "this works after a Free Study session too" clause is
unreachable in the current app. `SessionViewModel.close()` returns early for a Free Study session,
so it never reaches `.finished` and never shows a Summary — closing goes straight Home. The wiring
handles it correctly if such a summary ever exists. Worth its own task if a Free Study session
should end on a summary.

Checks: xcodegen generate, swiftformat --lint, swiftlint --strict, full xcodebuild test (301 unit +
2 UI) green on Kakitori-Sim. Owner verified the flows on device.
