
---
failed:blocked T077 — scope-creep: diff touched files outside declared scope (test-output.txt) — the task's scope is likely too narrow or wrong; fix the scope (or split the task), then re-open

---
2026-07-20 — hand-built (manual recovery). The loop's earlier attempt blocked on SPURIOUS
scope-creep: the builder redirected xcodebuild output to `test-output.txt` in the repo root,
which wasn't gitignored, so it entered the diff and tripped the first-out-of-scope-edit gate.
Root cause fixed separately (gitignored root-level build/test logs).
Implemented: SessionQueue gains a FIXED `initialCount` denominator (distinct cards at build) and a
monotonic `completedCount` incremented in markGraded only when a card leaves for good (post-grade
state NOT learning/relearning) — never on "Again" (re-queued). SessionViewModel exposes
completedCount/sessionCardCount; SessionView's done/left/progressBar read completions, not
gradeCounts. New Tests/UnitTests/SessionQueueProgressTests.swift (7 tests) asserts: Again does not
increment; a graduating grade increments once; a graded .review card increments once; multiple
Agains then graduation counts once; completed reaches but never exceeds the denominator.
DoD: xcodegen + swiftformat --lint + swiftlint --strict + xcodebuild test on Kakitori-Sim all
green (full suite). Verified on BOTH Kakitori-Sim (iPad) and Kakitori-Sim-Phone (iPhone): at start
"0 done · N left" empty bar; after grade-again header stays "0 done" and bar unmoved; after a
graduating grade header reads "1 done" and the bar advances. (The pre-fix main build reproduced
the bug — bar advanced on Again.) status set done in this commit (hand-build).
