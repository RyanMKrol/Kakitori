# T006 — SwiftData model: CardSchedule + CardState

## 2026-07-17

Added `Sources/Models/CardSchedule.swift` per docs/05-architecture.md §3 and the initial values from
docs/03-srs-algorithm.md §2: `enum CardState: String, Codable { case new, learning, review,
relearning }` and `@Model final class CardSchedule` with `state`, `stepIndex`, `easeFactor`,
`intervalDays`, `dueAt: Date?`, `lapses`, and `@Relationship(inverse: \Note.schedule) var note:
Note?`. `init` defaults: `state = .new`, `stepIndex = 0`, `easeFactor = 2.5`, `intervalDays = 0`,
`dueAt = nil`, `lapses = 0`. Dropped the explicit `Sendable` conformance on `CardState` — as with
`Script` in T005, SwiftFormat's `redundantSendable` rule flags it as redundant for a non-public
raw-value enum (implicitly Sendable already).

Edited `Sources/Models/Note.swift`: added `var schedule: CardSchedule?` (plain var, defaulting to
`nil`), with the inverse declared on `CardSchedule`.

Added `Tests/UnitTests/CardScheduleModelTests.swift` (XCTest) against an in-memory
`ModelContainer(for: Deck.self, Section.self, Note.self, CardSchedule.self, configurations:
ModelConfiguration(isStoredInMemoryOnly: true))`:
- `testDefaultInitialValues` asserts `CardSchedule()` has `.new`, `stepIndex == 0`,
  `easeFactor == 2.5`, `intervalDays == 0`, `dueAt == nil`, `lapses == 0`.
- `testNoteScheduleLinkRoundTripsBothDirections` inserts a `Note` and a `CardSchedule`, links
  `note.schedule = schedule`, saves, refetches the note, and asserts the schedule round-trips with
  `schedule.note === note`.
- `testNonDefaultValuesRoundTrip` saves a `CardSchedule(state: .review, intervalDays: 12.0,
  dueAt: fixedDate, lapses: 2)` and asserts every value round-trips unchanged after refetch.

Checks run (all green): `./tools/ensure-dod-tools.sh`, `xcodegen generate`,
`./tools/bin/swiftformat Sources Tests --lint` (0/11 files need formatting),
`./tools/bin/swiftlint --strict` (0 violations), and
`xcodebuild test -project Kakitori.xcodeproj -scheme Kakitori -destination "name=Kakitori-Sim"
CODE_SIGNING_ALLOWED=NO` — 14 unit tests + 1 UI test, all passed (**TEST SUCCEEDED**).

Nothing remaining for this task.
