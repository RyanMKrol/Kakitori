# T009 — DEBUG demo seed behind -seedDemoData launch argument

## Worklog

**2026-07-17 — Completed**

### What I did

Implemented the demo seed system for DEBUG builds behind the `-seedDemoData YES` launch argument.

**Created `Sources/App/DemoSeed.swift`:**
- `DemoSeed` enum with `seedIfNeeded()` and `seed()` functions
- Gate: checks UserDefaults for "seedDemoData" flag (set by launch args)
- Idempotent: checks for existing Demo::Hiragana deck before creating
- Seeds 3 decks with deterministic demo data:
  - Hiragana: 4 notes (あ/い/う/え)
  - Katakana: 3 notes (ア/イ/ウ)
  - Kanji: 3 notes (日/月/語)
- Mixed schedule states across 10 notes: .new, .learning, .review
- 3 consecutive DailyStats (today, yesterday, 2 days ago) via clock.adjustedDay()
- All times use injected AppClock, never bare Date()

**Updated `Sources/App/KakitoriApp.swift`:**
- Added init() to call DemoSeed.seedIfNeeded() in DEBUG builds at startup

**Created `Tests/UnitTests/DemoSeedTests.swift`:**
- 5 unit tests with in-memory SwiftData container
- Validates: deck/note/stats counts, deck titles, mixed schedule states, idempotence, day strings

### Checks run

✓ Format: `./tools/bin/swiftformat Sources Tests --lint` — passed
✓ Lint: `./tools/bin/swiftlint --strict` — 0 violations
✓ Tests: `xcodebuild test` — all 131 unit + 1 UI test passed
✓ Visual: `./build_run.sh Kakitori-Sim -seedDemoData YES` — Home displays 3 decks (ひらがな/カタカナ/漢字), not empty state
✓ Idempotence: re-run with `-seedDemoData YES` shows exactly 3 decks still

### Scope

- ✓ Created `Sources/App/DemoSeed.swift`
- ✓ Modified `Sources/App/KakitoriApp.swift`
- ✓ Created `Tests/UnitTests/DemoSeedTests.swift`
- ✓ All changes within `Sources/App/` and `Tests/` scopes
- ✓ Test file added (required for expectsTest task)

### Next

Build passes DoD completely. Ready for loop to merge and push.
