# T078 — Settings: restyle on-brand + make numeric fields obviously editable

## 2026-07-20 13:43

**Done.** Completed the Settings screen visual polish task.

### Work Completed

Restyled `Sources/Features/Settings/SettingsView.swift` to:

1. **Rounded-card styling**: Wrapped the settings VStack with the app's established card language:
   - `background(KakitoriTheme.surface)` — white elevated card surface
   - `clipShape(RoundedRectangle(cornerRadius: KakitoriTheme.radiusMedium))` — 16pt corner radius
   - `.overlay(...stroke(KakitoriTheme.boxLine, lineWidth: 1))` — 1pt hairline border
   - Matches the visual language of DeckCardView, SummaryView, and other cards

2. **Numeric field affordance**: Made the two text inputs obviously editable:
   - Both "New cards per day" and "Max reviews per day" TextFields now render inside a small rounded pill
   - Applied `.padding(.horizontal, 10)` + `.padding(.vertical, 6)` for pill spacing
   - `.background(KakitoriTheme.inkFaint)` — subtle 12% opacity ink fill
   - `.clipShape(RoundedRectangle(cornerRadius: 8))` — 8pt rounded pill shape
   - Clear visual affordance that differs from static text, signaling "tap to edit"

3. **All functionality preserved**:
   - Four settings intact in order: New cards per day, Max reviews per day, Audio autoplay, Show romaji
   - @AppStorage keys unchanged: newCardsPerDay (default 10), maxReviewsPerDay (default 100), audioAutoplay (default true), showRomaji (default true)
   - Clamp ranges unchanged: 1...50 for new cards, 10...500 for max reviews
   - Number pad keyboard type (.keyboardType(.numberPad)) preserved
   - Commit-on-blur and onSubmit behavior unchanged
   - All accessibility identifiers preserved: settings-new-per-day, settings-max-reviews, settings-autoplay, settings-romaji
   - No Steppers reintroduced

### Checks Passed

- ✅ **Format**: `./tools/bin/swiftformat Sources Tests --lint` — 0 files require formatting
- ✅ **Lint**: `./tools/bin/swiftlint --strict` — 0 violations in 87 files
- ✅ **Tests**: `xcodebuild test` — 2/2 UI tests passing (LaunchTests, SmokeFlowTests)
- ✅ **Build**: `xcodebuild build` — SUCCESS
- ✅ **Scope**: Only file changed is `Sources/Features/Settings/SettingsView.swift` (within scope)

### Visual Verification

The Settings screen now presents:
- A rounded card containing all four settings (on-brand surface + boxLine stroke, matching Home and Summary screens)
- Two numeric input fields with a subtle pill affordance (inkFaint background, rounded, clearly tappable vs. static text)
- Clean, consistent visual hierarchy aligned with app's design language
- Fully functional iOS 18 / Swift 6 compliant

All preserved functionality verified to work correctly; no regressions introduced.
