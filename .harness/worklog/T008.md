# T008 worklog

## 2026-07-17

**Build complete.** Rewrote `Sources/App/KakitoriApp.swift`:
- `@main struct KakitoriApp: App` now builds a real `ModelContainer` over all five `@Model`
  types (`Deck`, `Section`, `Note`, `CardSchedule`, `DailyStats`) via a throwing static helper
  (`makeModelContainer()`), wrapped in `do/catch` — `fatalError` on failure, no `try!`.
- `WindowGroup { NavigationStack { HomeView() } }` with `.modelContainer(container)`.
- Removed the placeholder `Text("Kakitori")` / `placeholder-title` identifier.

Created `Sources/Features/Home/HomeView.swift`:
- `@Query private var decks: [Deck]`, background `KakitoriTheme.paper`.
- Header (`home-header`): 書 mark in `KakitoriTheme.japaneseDisplayFont` inside an
  accent-filled `Circle`, beside "Kakitori" + tagline "Write your Japanese, card by card".
- Empty state (`home-empty-state`, shown when `decks.isEmpty`): large faint 書 mark
  (`KakitoriTheme.inkFaint`) + "Import a deck to start writing" in `KakitoriTheme.ink`.
- Populated state (`home-deck-list`): plain `List`/`ForEach` showing `jpTitle ?? name` in the
  Japanese display font with `name` as a caption.

**Definition of Done:**
- Generated project: `xcodegen generate`
- Format: `./tools/bin/swiftformat Sources Tests --lint` — 0/39 files require formatting
- Lint: `./tools/bin/swiftlint --strict` — 0 violations in 39 files
- Test + build: `xcodebuild test -project Kakitori.xcodeproj -scheme Kakitori -destination "id=<Kakitori-Sim UDID>" CODE_SIGNING_ALLOWED=NO` — 126 unit tests + 1 UI test, 0 failures, BUILD SUCCEEDED
- Visual verification: `./build_run.sh` (dedicated `Kakitori-Sim` iPad) — observed the app fill
  the full screen (no letterboxing), header with vermillion accent circle + 書 glyph + "Kakitori"
  + tagline rendering correctly, and the empty-state faint 書 mark + "Import a deck to start
  writing" message on the fresh (empty) in-memory-equivalent on-device store. No default-blue
  SwiftUI tint anywhere; palette matches `docs/06-ui-design.md`.

Remaining: real stats row, deck cards, and Import UI are later tasks per the spec overview.
