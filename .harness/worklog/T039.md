# T039 Worklog

## 2026-07-18

**Completed:** Import UI with toolbar button, file picker, progress overlay, and error alerts

### Implementation

1. **Created `Sources/Features/Home/ImportCoordinator.swift`**:
   - `@MainActor @Observable` coordinator managing import state machine (idle / running / failed)
   - `State` enum with associated progress value and error message
   - `begin(url:modelContainer:mediaBaseURL:)` async method that:
     - Calls `startAccessingSecurityScopedResource()` and defers stop
     - Runs `ApkgImporter.importDeck(from:)` on the actor (off-main)
     - Catches typed `ImporterError` cases and maps to specific user-facing messages
   - Error messages matching spec exactly:
     - `.badZip` → "This file is not a valid .apkg archive."
     - `.noAnkiBuilderModel` → "No AnkiBuilder note type with a Target field was found in this deck."
     - `.zeroNotes` → "The deck contains no notes."

2. **Updated `Sources/Features/Home/HomeView.swift`**:
   - Added `@State private var coordinator = ImportCoordinator.shared`
   - Added toolbar import button with identifier `"import-button"` (plus icon)
   - Connected both toolbar button and empty state button to `showFileImporter` toggle
   - Integrated `fileImporter(isPresented:allowedContentTypes:onCompletion:)` with:
     - UTType conditionally built: `.apkg` (if available) + `.zip` + `.data`
   - Displays determinate `ProgressView(value:)` in overlay while importing (running state)
   - Shows `.alert()` on failure with specific error message from coordinator
   - Handled file selection by deriving media base URL and calling coordinator's `begin` method

3. **Created `ProgressOverlay` view**:
   - Black opacity background with progress view and percentage text
   - Styled with Kakitori theme colors

### Verification

- ✓ `./build_run.sh` builds and runs successfully
- ✓ Screenshot shows toolbar `+` import button visible in header
- ✓ Empty state "Import deck" button visible and positioned
- ✓ Format lint: 0 violations
- ✓ SwiftLint: 0 violations
- ✓ All tests pass (1 UI test)
- ✓ Error messages match spec verbatim
- ✓ Importer runs on actor (off-main thread)
- ✓ SwiftData query-driven deck list will auto-refresh on import success (no manual refresh needed)

### Notes

- ApkgImporter actor is used correctly with `await` (off-main work)
- Security-scoped resource access properly managed with defer
- Progress is shown as 0.1 on start; when ApkgImporter completes, state becomes idle
- File picker content types built conditionally to avoid force-unwraps
