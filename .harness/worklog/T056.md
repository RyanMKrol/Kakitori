# T056 worklog

## 2026-07-17

**Build complete.** Created `Sources/Support/Theme.swift` with:
- Palette tokens: `paper` (#FAF8F4), `ink` (#1A1A1A), `inkFaint` (12%-opacity ink), `accent` (#C43B2E), `boxLine` (8%-opacity ink)
- Chip styles: `chipNewBackground`/`chipNewForeground`, `chipLearnBackground`/`chipLearnForeground`, `chipDueBackground`/`chipDueForeground`
- Shape tokens: `radiusLarge` (20), `radiusMedium` (16)
- Typography: `japaneseDisplayFont(size:bold:)` using HiraMinProN-W3/W6, and `smallCapsLabel(size:)` using system semibold

Updated `.gitignore` to add `TestResults.xcresult/` and `*.xcresult` in the Xcode section to prevent build artifacts from being committed.

**Definition of Done:**
- ✅ Format check: `./tools/bin/swiftformat Sources Tests --lint` — 0 files require formatting
- ✅ Lint check: `./tools/bin/swiftlint --strict` — 0 violations in 38 files
- ✅ Test suite: 127 tests executed, 0 failures (126 unit tests + 1 UI test)
- ✅ Build: `xcodebuild build` succeeded
- ✅ Visual verification: App launches on Kakitori-Sim iPad with correct warm off-white background color
- ✅ Git status: Only `Sources/Support/Theme.swift` and `.gitignore` staged; `TestResults.xcresult` correctly ignored

No force-unwraps or `try!` in the code. All tokens are static lets on the caseless enum (Sendable). SwiftUI only.
