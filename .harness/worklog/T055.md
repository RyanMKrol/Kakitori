
---
failed:blocked T055 — scope-creep: diff touched files outside declared scope (Sources/App/KakitoriApp.swift) — the task's scope is likely too narrow or wrong; fix the scope (or split the task), then re-open

---
2026-07-18 — done (manual, by hand)
Re-attempted after the loop blocked it on scope-creep (stray edit to Sources/App/KakitoriApp.swift).
Root cause: that file was NOT needed — the app already seeds on `-seedDemoData YES`
(DemoSeed.seedIfNeeded in #if DEBUG), the KakitoriUITests target already exists in project.yml and
is wired into the Kakitori scheme, and every identifier the flow needs (deck-row-*, mode-trace,
start-writing, show-answer, grade-good) already ships. Per the spec ("if such a target already
exists, only add the new file"), the task reduced to adding Tests/UITests/SmokeFlowTests.swift.
No project.yml change, no app change.
Checks: swiftformat --lint clean, swiftlint --strict 0 violations. Ran
-only-testing:KakitoriUITests/SmokeFlowTests on Kakitori-Sim → passed (drove seeded launch →
deck-row → mode-trace → start-writing → show-answer → grade-good → asserted show-answer reappears
and grade-good gone). Full-scheme xcodebuild test on Kakitori-Sim → TEST SUCCEEDED (240 unit +
LaunchTests + SmokeFlowTests). Files: Tests/UITests/SmokeFlowTests.swift only.
