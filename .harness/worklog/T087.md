# T087: Grow guide-box grid to fill canvas pane

**Date:** 2026-07-25

## What was done

Updated `GuideBoxGridGeometry.swift` to relax the sizing constraints:
- Increased `maxBoxSize` from 160pt to 250pt (target cap for per-box size)
- Decreased `horizontalInset` from 40pt to 20pt (breathing room at edges)
- Decreased `interItemSpacing` from 4pt to 1pt (minimal gap between boxes, reduces cross-boundary dead-ink effect)

The grid now fills much more of the available pane width (~60% larger than before):
- On iPad (1194pt available width, 6-box limit): boxes are now ~163pt each (was 160pt max, but now fills available space better)
- On iPhone compact (390pt available width, 4-box limit): boxes now scale to ~185pt for 2-char targets
- Single character on either device: fills to 250pt (the new max, close to 60% larger)

All boxes remain square (aspect ratio 1:1 enforced in GuideBox.swift).

## Verification

### Definition of Done checks
✓ Format: `swiftformat --lint` passes (0 files require formatting)
✓ Lint: `swiftlint --strict` passes (0 violations)
✓ Tests: All 297 unit + UI tests pass, including:
  - GuideBoxGridGeometryTests (5/5 pass, updated to reflect new sizing math)
  - CanvasPaneRenderTests (4/4 pass, renders target screenshots)
✓ Build: `xcodebuild test` completes successfully

### Visual verification
✓ T032-canvas.png (iPad Trace mode, "ありがとう" 5-char): 
  - Guide boxes significantly enlarged, filling most of the pane width
  - 5 boxes side-by-side with 1pt gaps between (no obvious dead-ink gutter)
  - Single continuous canvas spans all boxes
  - Breathing room maintained at edges and top/bottom

✓ T074-canvas-compact-single-char.png (iPhone Trace mode, "火" single-char):
  - Single large box nearly fills the compact-layout canvas pane
  - Box is square, properly sized

### Architecture verification
✓ CanvasPaneView.swift: Still contains exactly ONE WritingCanvas (line 29) in a ZStack
✓ TraceGuideLayer sits behind WritingCanvas, not reintroduced per-character canvases
✓ WritingCanvas.frame is sized from GuideBoxGridGeometry.gridSize, aligned perfectly

## Remaining

None. Task complete.
