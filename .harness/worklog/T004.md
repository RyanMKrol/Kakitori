# T004 — SwiftData models: Deck + Section

## 2026-07-17

Added `Sources/Models/Deck.swift` with the two `@Model` types per docs/05-architecture.md §3:
`Deck` (`id`, `name`, `jpTitle: String?`, `sourceDeckName`, `importedAt`, and a `sections`
to-many relationship with `deleteRule: .cascade` and inverse `\Section.deck`) and `Section`
(`id`, `name`, `orderIndex`, plain `deck: Deck?` back-reference). `sections` carries a `= []`
default per the project's SwiftData lint hook (to-many relationships need a default). Neither
`init` reads `Date()` — `importedAt` is always caller-supplied. No `Note` type referenced, no
force-unwraps.

Added `Tests/UnitTests/DeckModelTests.swift` (XCTest) against an in-memory
`ModelContainer(for: Deck.self, Section.self, configurations: ModelConfiguration(isStoredInMemoryOnly: true))`:
- `testDeckScalarFieldsRoundTrip` inserts a `Deck` with all five scalar fields set, saves,
  refetches, and asserts each round-trips unchanged.
- `testSectionsRoundTripWithInverseRelationship` appends two `Section`s to `deck.sections`,
  saves, refetches the deck, and asserts `sections.count == 2` and each section's `deck`
  back-reference resolves to the same deck (both relationship directions).

Checks run (all green): `./tools/ensure-dod-tools.sh`, `xcodegen generate`,
`./tools/bin/swiftformat Sources Tests --lint` (0/7 files need formatting),
`./tools/bin/swiftlint --strict` (0 violations), and
`xcodebuild test -project Kakitori.xcodeproj -scheme Kakitori -destination "id=<Kakitori-Sim UDID>" CODE_SIGNING_ALLOWED=NO`
— 8 unit tests + 1 UI test, all passed (**TEST SUCCEEDED**).

Nothing remaining for this task; `Note`/`notes` on `Section` is explicitly deferred to a later
task per the spec.
