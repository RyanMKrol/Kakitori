
---
failed:blocked T090 — exhausted 2 attempts at the top model rung

## 2026-07-25 — done (by hand, off-loop)

Built manually with the owner verifying on device, after the loop exhausted its attempts.

The caught-up state now renders inline on the deck setup sheet: heading, secondary line, the
practice-mode picker left visible, and a blue "Start Free Study" button that launches an endless
session in whichever mode is selected. The full-screen "All caught up" interstitial is gone; an
empty seen pool is handled by a silent guard in `startFreeStudySession`, with no screen.

One bug surfaced during verification and was fixed in a follow-up commit: Home and the sheet
disagreed about "caught up". The deck card compared progress against the day's snapshotted target
while the sheet asked whether the live allowance was empty, so a deck could read "All caught up" on
Home and "4 cards to study · Start writing" on the sheet. Both now go through a single
`DailyAllowance.isDayComplete`.

Why the loop couldn't finish this: the smoke UI test tapped the first deck row and assumed
"Start writing". Once that deck was done for the day it offered Free Study instead, so the local
DoD gate went red regardless of what the attempt wrote. The test now walks the rows until it finds
a deck with cards due.

Checks: xcodegen generate, swiftformat --lint, swiftlint --strict, and the full xcodebuild test
(301 unit + 2 UI) all green on Kakitori-Sim. Verified visually on both Kakitori-Sim (iPad) and
Kakitori-Sim-Phone.
