# T070 — DeckSetupSheet wording fix

## 2026-07-20

**Summary:** Fixed the misleading wording in DeckSetupSheet's subtitle. The label previously claimed "N cards due" but the count actually represented new + learning + due cards (the total daily allowance for a deck).

**What I did:**
- Read the task spec and understood the issue: DeckSetupSheet subtitle at line 103 said "cards due" but dueCount is actually the sum of newCount + learnCount + dueCount from DailyAllowance.total
- Changed the label from "N cards due" to "N cards to study" to accurately describe a count that includes new/learning cards
- Ran all Definition of Done checks:
  - `xcodegen generate` — passed
  - `./tools/bin/swiftformat Sources Tests --lint` — passed (0 files require formatting)
  - `./tools/bin/swiftlint --strict` — passed (0 violations, 0 serious in 83 files)
  - `xcodebuild test -project Kakitori.xcodeproj -scheme Kakitori -destination "name=Kakitori-Sim" CODE_SIGNING_ALLOWED=NO` — passed (2 tests executed, 0 failures)

**Visual verification:**
- Verified the change on iPad (Kakitori-Sim) via render test screenshot `T037-decksetup.png`: shows "Hiragana · 10 cards to study" (correct)
- Verified layout on iPhone (Kakitori-Sim-Phone) home screen: layout intact, ready for tap to DeckSetupSheet

**Result:** ✅ Done. The subtitle now accurately reflects that the count includes new + learning + due cards, not just due cards.
