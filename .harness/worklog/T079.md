# T079: SummaryView compact button labels swapped

## Work done (2026-07-20)

**Task:** Swap the compact-branch labels in SummaryView's two buttons so each label matches its action.

**Changes made:**
1. **SummaryView.swift** (Sources/Features/Summary/SummaryView.swift):
   - Line 108: Button `onBackToDecks` action — changed compact label from "Study another script" to "Back home"
   - Line 119: Button `onStudyAnother` action — changed compact label from "Back home" to "Study another script"

**Verification:**
- Format check: ✓ PASS (`swiftformat --lint`)
- Lint check: ✓ PASS (`swiftlint --strict`)
- Tests: ✓ PASS (UI tests + new unit test for compact render)
  - Full test suite executed: 2 UI tests + all unit tests
  - New test added: `SummaryCompactRenderTests.testSummaryCompactRender()` generates compact screenshot
- Build: ✓ PASS (`xcodebuild test`)

**Visual verification:**
- iPad (regular) layout screenshot (T034-summary.png): Buttons unchanged, read "Back to decks" (accent) and "Study another deck" (outlined) ✓
- iPhone (compact) layout screenshot (T079-summary-compact.png): Buttons now correctly read "Back home" (accent, goes home) and "Study another script" (outlined, re-opens deck setup) ✓

**Scoping:**
- Only edited: `Sources/Features/Summary/SummaryView.swift` (label text, lines 108 and 119)
- Added test file: `Tests/UnitTests/SummaryCompactRenderTests.swift`
- Did NOT edit: HomeView.swift (action callbacks already correct), button accessibility identifiers, ordering, styling, or non-compact labels
- Did NOT edit: README.md, TASKS.json, or any other out-of-scope files

**Result:** DONE. Both device layouts now have correct labels matching their actions.
