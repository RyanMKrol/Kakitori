# T043 — Translate & Write mode (翻訳)

## 2026-07-18 08:12 UTC+1 — Implementation complete

### What was built
Implemented the Translate & Write session prompt for PracticeMode.translate:

1. **Prompt pane (PromptPaneView.swift)**
   - Added `translateModePrompt` view with:
     - Accent small-caps caption "TRANSLATE & WRITE"
     - Secondary-color field label "English" (gray, reduced opacity)
     - English meaning as bold 34pt headline, left-aligned
     - Sub-line "Write this in Japanese." (exact copy from spec)
     - All text left-aligned at top of pane (matches iPad left-pane style)
   - Answer block (after Show answer) unchanged — uses standard answer display

2. **Canvas pane (CanvasPaneView.swift)**
   - Added canvas hint for translate mode: "Write the Japanese here"
   - Empty guide boxes (no faded trace glyphs) — trace guides only render for `.trace` mode
   - TraceGuideLayer already respects isVisible flag (only passes glyphs when true)

3. **Tests added**
   - PromptPaneRenderTests: `testTranslateModePromptBeforeReveal`, `testTranslateModeAnswerAfterReveal`
   - CanvasPaneRenderTests: `testTranslateModeCanvasHidesGuideBoxes`
   - All tests pass; screenshots generated: T043-translate-prompt.png, T043-translate-answer.png, T043-canvas-translate.png

### Verification
- ✓ Format: `./tools/bin/swiftformat Sources Tests --lint` (0 files require formatting)
- ✓ Lint: `./tools/bin/swiftlint --strict` (0 violations)
- ✓ Build: `xcodebuild test … -destination id=$SIM … CODE_SIGNING_ALLOWED=NO` (BUILD SUCCEEDED)
- ✓ Unit + UI tests: 204 tests passed (203 unit, 1 UI launch)
- ✓ Visual verification: screenshots confirm:
  - "TRANSLATE & WRITE" caption in accent red small caps
  - "English" label in secondary gray
  - English value ("fire") rendered bold, large, left-aligned
  - "Write this in Japanese." sub-line present, exact copy
  - Canvas hint correct
  - Guide boxes empty (no faded glyphs in translate mode)
  - Target (火) nowhere visible before Show answer

### Notes
- The implementation leverages existing architecture: TraceGuideLayer already has the `isVisible` flag, and CanvasPaneView already handles hint text per mode via switch
- No changes to PromptPaneView's answer block needed — the standard answer block displays target, reading, English, hint, and play-audio button, all of which are appropriate for Translate & Write
- The nil-coalescing `viewModel.currentNote?.english ?? ""` safely handles missing English; blank rendering is acceptable per spec ("a nil or empty `english` may render blank in this task — availability filtering lands in T045")

### Scope
Changed files:
- Sources/Features/Session/PromptPaneView.swift — added translateModePrompt case
- Sources/Features/Session/CanvasPaneView.swift — added translate mode hint text
- Tests/UnitTests/PromptPaneRenderTests.swift — added two test methods
- Tests/UnitTests/CanvasPaneRenderTests.swift — added one test method

All changes within scope. No README.md changes (product doc, not buildable task output). No TASKS.json edits (loop owns status).
