# T040 — Register .apkg document type (Open in Kakitori)

## Completed 2026-07-18

### What was done

1. **Updated `project.yml`** to declare the .apkg UTI and document type under the Kakitori target's `info.properties`:
   - `UTImportedTypeDeclarations`: registered `com.ryankrol.kakitori.apkg` as a zip-archive conforming type with `.apkg` filename extension
   - `CFBundleDocumentTypes`: declared the Kakitori app as a Viewer for `.apkg` files with Alternate handler rank

2. **Regenerated project** with `xcodegen generate` to produce `Sources/Info.plist` with the new declarations verified by plist extraction

3. **Updated `KakitoriApp.swift`** to attach `.onOpenURL` handler:
   - Handler attached to the NavigationStack within the WindowGroup
   - Calls private `handleOpenURL(_:)` helper that computes the media base URL (same path as HomeView's file importer handler)
   - Routes incoming URLs to `ImportCoordinator.shared.begin(url:modelContainer:mediaBaseURL:)` in a Task

### Checks run

- ✅ Format check: `swiftformat Sources Tests --lint` — 0 files require formatting
- ✅ Lint check: `swiftlint --strict` — 0 violations, 0 serious issues in 57 files
- ✅ Unit tests: `xcodebuild test` — 144 unit tests passed
- ✅ UI tests: 1 launch test passed
- ✅ Build: `./build_run.sh Kakitori-Sim -seedDemoData YES` — succeeded, app launched clean
- ✅ Home screen renders with all decks and stats visible (screenshot verified)
- ✅ Plist verification: both `CFBundleDocumentTypes` and `UTImportedTypeDeclarations` present with correct structure and values

### Notes

The handler correctly mirrors HomeView's import flow: it accesses the same media base URL location and calls the same ImportCoordinator method that already has progress UI and error alert handling. No new view code needed — the share-sheet integration is now purely a plist + app-level routing change.
