# T013 Worklog

## 2026-07-18 — Complete

**Completed:** Trace guide layer component rendering faded glyphs under the canvas.

### Changes

1. **Extended `GuideBox.swift`** with optional `traceGlyph: String?` parameter
   - When provided, renders glyph centred in box using `Font.custom("Hiragino Mincho ProN", size: boxSide * 0.72)`
   - Glyph opacity set to 12% ink (`KakitoriTheme.ink.opacity(0.12)`)
   - Layout reuses existing box border + dashed cross styling; glyphs drawn as SwiftUI layer, NOT added to PKDrawing

2. **Extended `GuideBoxRow.swift`** with `traceGlyphsVisible: Bool = false` parameter
   - Extracts glyph string from `.box()` units and passes to `GuideBox`
   - Leaves `.inline()` punctuation always visible (never faded)

3. **Created `TraceGuideLayer.swift`**
   - `struct TraceGuideLayer: View` wrapping `GuideBoxRow`
   - Accepts `units: [SegmentedUnit]`, `maxBoxesPerRow: Int = 6`, `isVisible: Bool`
   - Doc comment clarifies intended ZStack composition: guides behind, canvas in front
   - Assigned `accessibilityIdentifier = "trace-guide-layer"`
   - Includes two `#Preview` variants (visible + hidden)

4. **Added preview snapshot tests** to `WritingCanvasPreviewSnapshotTests.swift`
   - `testTraceGuideLayerVisibleRendersToImage()` — verifies glyphs render faded in boxes
   - `testTraceGuideLayerHiddenRendersToImage()` — verifies empty boxes render when `isVisible: false`

### Verification

**Definition of Done:**
- ✓ Format: `./tools/bin/swiftformat Sources Tests --lint` — 0 violations
- ✓ Lint: `./tools/bin/swiftlint --strict` — 0 violations
- ✓ Tests: `xcodebuild test` — 137 unit tests + 1 UI test all pass
- ✓ Build: Generated `Kakitori.xcodeproj` from `project.yml`, compiled without errors

**Visual Verification:**
- ✓ `testTraceGuideLayerVisibleRendersToImage()` rendered to PNG: faded serif あ り が と う glyphs centered one-per-box, 12% ink opacity, hairline borders + dashed cross-lines visible
- ✓ `testTraceGuideLayerHiddenRendersToImage()` rendered to PNG: same boxes empty, only borders + dashed lines visible

**Code Inspection:**
- ✓ Glyphs drawn by SwiftUI layer (never added to `PKDrawing`), so `WritingCanvasController.clear()` cannot erase guides
- ✓ Layout reused from `GuideBoxRow` — no duplication of box positioning logic
- ✓ Swift 6 strict concurrency clean; no force-unwraps in Sources
- ✓ Font is Hiragino Mincho ProN (serif, as spec requires), not sans

### Status

`done` — all Definition of Done checks passed, previews render correctly.
