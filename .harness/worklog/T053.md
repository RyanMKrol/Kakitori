# T053 — Empty states + import error polish

## 2026-07-18

**What I did**

- Confirmed Home's empty state (`Sources/Features/Home/HomeView.swift`) already matched
  docs/06 §2.1 exactly (書 mark, "Import a deck to start writing", centered Import deck
  button, `home-empty-state` identifier) — no change needed there beyond wiring the
  in-progress state (below).
- `TodayBannerView.swift`: banner is no longer hidden when nothing is due — it now always
  renders and shows "All caught up. Nothing due right now." when total due across all decks
  is zero, otherwise the existing "N characters to write · across M scripts" copy. Extracted
  the due-count calculation into a static `calculateDueCards(decks:now:)` for direct unit
  testing.
- `DeckCardView.swift`: added `isAllCaughtUp` (new + learning + due all zero) and replaced
  the chips row with a quiet "All caught up" label in that case; the Study button remains
  enabled (ahead-of-schedule study stays allowed) — no change to `onStudy` wiring.
- `ImportCoordinator.swift`: mapped `ImporterError` cases to the exact user-facing strings
  from the task spec (badZip → "This file could not be read as an Anki deck.",
  noAnkiBuilderModel → "This deck is missing the Target field Kakitori needs.", zeroNotes →
  "This deck has no cards to import."). Exposed the mapping as a `nonisolated static`
  `errorMessage(for:)` so it's directly unit-testable outside the main actor.
- `HomeView.swift`: gave the import failure alert the `import-error-alert` accessibility
  identifier (title stays "Import failed", already present). Added an in-progress state to
  both import affordances (header `+` button and empty-state "Import deck" button): while
  `ImportCoordinator.state` is `.running`, each shows a `ProgressView` and is `.disabled`
  until the import succeeds or the failure alert appears.
- Added `Tests/UnitTests/HomeEmptyStatesTests.swift`: asserts the three error-message
  mappings, `TodayBannerView.calculateDueCards` returning 0/0 when nothing is due vs.
  counting new+overdue-review cards otherwise, and `DeckCardView.isAllCaughtUp` for both the
  zero-counts and has-new-cards cases.

**Checks run (all green)**

- `./tools/ensure-dod-tools.sh`
- `xcodegen generate`
- `./tools/bin/swiftformat Sources Tests --lint` — 0/58 files require formatting
- `./tools/bin/swiftlint --strict` — 0 violations
- `xcodebuild test -project Kakitori.xcodeproj -scheme Kakitori -destination "id=$(./tools/loop_sim.sh)" CODE_SIGNING_ALLOWED=NO` —
  all 149 unit tests + the UI launch test passed
- Visual verification on `Kakitori-Sim` (iPad): uninstalled the app, ran `./build_run.sh
  Kakitori-Sim` with no seed data — observed the 書 mark, "Import a deck to start writing",
  and a centered Import deck button, matching docs/06 §2.1. Then ran `./build_run.sh
  Kakitori-Sim -seedDemoData YES` — observed the normal populated Home (today banner with
  non-zero due count, deck cards with their usual chip rows), confirming the all-caught-up
  variants don't fire when there's real due work.

**What remains**

- Nothing outstanding for this task's scope. The all-caught-up visual states (banner "All
  caught up. Nothing due right now." and a deck card's quiet "All caught up" label) were
  verified via unit tests only, not a screenshot — the seeded demo data always has non-zero
  due counts, and manufacturing an all-zero-count seeded state was out of scope for this task.
