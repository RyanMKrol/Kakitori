# T089 worklog

## 2026-07-25

Added the shared Free Study tokens to `Sources/Support/Theme.swift`:

- `KakitoriTheme.freeStudyAccent`: a dynamic blue `Color` (light `#286ED2`, dark `#5A96EB`),
  placed next to the existing `accent` token, resolved via a `UIColor` trait closure like
  `paper`/`surface` so it stays legible in both appearances. Clearly distinct from the
  vermillion `accent`.
- `KakitoriTheme.freeStudyLabel = "Free Study"`: the single shared on-screen label constant
  for the feature name, for the deck-setup button, Summary button, and running-session badge
  to consume identically (wired up in fsep-b/fsep-c, not this unit).

No other files touched, per scope.

### Checks run

- `xcodegen generate` — regenerated project, clean.
- `./tools/bin/swiftformat Sources Tests --lint` — 0/93 files require formatting.
- `./tools/bin/swiftlint --strict` — 0 violations, 0 serious, 93 files.
- `xcodebuild test -project Kakitori.xcodeproj -scheme Kakitori -destination 'platform=iOS Simulator,name=Kakitori-Sim' CODE_SIGNING_ALLOWED=NO` — **TEST SUCCEEDED**, all tests passed (unit + UI smoke).

No visual change ships in this unit (tokens only, unconsumed until fsep-b/fsep-c), so per the
task spec a screenshot verification is not required here.

### What remains

fsep-b and fsep-c consume `freeStudyAccent` and `freeStudyLabel` to wire up the actual UI
(Start Free Study button, Summary button, running-session badge) and carry the dual-device
visual verification for this feature.
