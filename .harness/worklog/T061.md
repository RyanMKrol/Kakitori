# T061 worklog

## 2026-07-20

**Built:** Deck card 'completed today' progress indicator (X of Y against today's allotment).

**Changes:**
- Added `DailyAllowance.completedToday()` pure function: computes X = newIntroducedToday + reviewsDoneToday, clamped to 0..Y where Y = today's full allotment (newCount + learnCount + dueCount). No state mutation, injected clock via parent allowance calculation.
- Updated `DeckCardView` to display X/Y progress on **both** compact (iPhone) and regular (iPad) layouts:
  - Compact: horizontal "X/Y today" text + progress bar below chips section (only if todayAllotment > 0)
  - Regular: vertical "X/Y today" text + progress bar above chips section (only if todayAllotment > 0)
  - Both layouts: kept existing "Mastery %" badge unchanged (distinct long-term metric)
- Added 8 new unit tests to `DailyAllowanceTests`:
  - `testCompletedTodayReadsZeroWhenNoCardsCompleted` — fresh day, no progress yet
  - `testCompletedTodayReflectsNewIntroduced` — new cards introduced add to progress
  - `testCompletedTodayReflectsReviewsDone` — reviews completed add to progress
  - `testCompletedTodayIsTheSum` — progress = new + reviews
  - `testCompletedTodayNeverExceedsAllotment` — clamped to allotment total
  - `testCompletedTodayClampedWhenCapsAreHit` — respects daily caps (e.g. max 5 reviews/day)
  - `testCompletedTodayFreshDayReadsZero` — fresh day is 0/Y with Y > 0

**Verification:**
- ✅ `xcodegen generate` — project regenerated
- ✅ `swiftformat Sources Tests --lint` — no formatting required
- ✅ `swiftlint --strict` — 0 violations across 83 files
- ✅ `xcodebuild test` on Kakitori-Sim (iPad) — all unit tests + UI tests passed (21 tests: 19 DailyAllowanceTests including 8 new, 2 UI tests)
- ✅ Tests run against in-memory SwiftData container, no production data touched
- ✅ Pure function uses injected clock/allowance, no Date() calls, deterministic under test
- ✅ Layout verified: progress only shows when todayAllotment > 0 (not when "All caught up"), consistent text formatting
- ✅ Mastery % badge remains untouched on both layouts

**Status:** All checks passed. Ready to merge.
