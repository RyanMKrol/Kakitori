# T024 — SM2Scheduler: review transitions, clamps, seeded fuzz

## 2026-07-18

**What I did:**

Extended `Sources/Scheduler/SM2Scheduler.swift` to handle `.review` state card grading with proper SM-2 algorithm implementation, interval clamping to [1, 365] days, and seeded ±5% fuzz for intervals ≥ 3 days.

**Implementation:**

1. Added `fuzzEnabled: Bool` parameter to `SM2Scheduler` (default `true`, optional override for previews)
2. Replaced the `.review` placeholder case in `apply()` with `applyReview()` method
3. Implemented exact SM-2 formulas per docs/03-srs-algorithm.md §4:
   - **again**: lapses += 1, EF = max(1.3, EF - 0.20), state = relearning, dueAt = now + 600s, intervalDays unchanged
   - **hard**: EF = max(1.3, EF - 0.15), newI = round(I × 1.2), clamped [1, 365]
   - **good**: EF unchanged, newI = round(I × EF), clamped [1, 365]
   - **easy**: EF = EF + 0.15, newI = round(I × newEF × 1.3), clamped [1, 365]
4. Fuzz applies ONLY to review cards with intervals ≥ 3 days: f = random(-0.05...0.05)
5. Refactored into helper methods to keep functions under the 50-line SwiftLint limit

**Tests created:**

Created `Tests/UnitTests/SM2ReviewTests.swift` with 10 XCTest test cases covering:
- All four grade transitions (again, hard, good, easy) with exact value verification
- Ease floor enforcement at 1.3 (never below)
- Interval clamping at maximum 365 days
- Fuzz bounds property test: 100 seeds, intervals ≥ 3 days fuzz within ±5%, distinctness verified
- No-fuzz verification: intervals < 3 days never fuzzed regardless of seed

**Definition of Done checks:**

- ✅ Format: `./tools/bin/swiftformat Sources Tests --lint` passed
- ✅ Lint: `./tools/bin/swiftlint --strict` passed (refactored into helper methods to comply)
- ✅ Tests: 10 new SM2ReviewTests added and passing
- ✅ Build: `xcodebuild test -project Kakitori.xcodeproj -scheme Kakitori -destination 'name=Kakitori-Sim' CODE_SIGNING_ALLOWED=NO`
  - 175 unit tests pass (includes all 10 SM2ReviewTests)
  - LaunchTests pass
  - TEST SUCCEEDED

All acceptance criteria from "## Done when" verified:
- Exact values match (I=10, EF=2.5, lapses=0): again (state relearning, lapses 1, EF 2.3, I 10, dueAt +600s), hard (EF 2.35, I 12, dueAt +1036800s), good (EF 2.5, I 25, dueAt +2160000s), easy (EF 2.65, I 34, dueAt +2937600s)
- Ease floor enforced (EF 1.4→1.3 on again, 1.35→1.3 on hard, etc.)
- Clamping works (I 300, EF 2.5, good → intervalDays 365, not 750)
- Fuzz bounds hold: 100 seeds each with dueAt in [now + 25×86400×0.95 ... now + 25×86400×1.05], at least 2 distinct values
- Intervals < 3d never fuzzed (I 2, hard with fuzz on gives exact dueAt +172800 for all 100 seeds)
