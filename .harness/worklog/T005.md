# T005 — SwiftData model: Note + Script classification

## 2026-07-17

Added `Sources/Models/Note.swift` per docs/05-architecture.md §3: the `Script` enum
(`hiragana | katakana | kanji | mixed`, `String, Codable`) and `@Model final class Note` with
`id`, `target`, `pronunciation: String?`, `english: String?`, `category: String?`, `hint: String?`,
`audioFilename: String?`, `script: Script`, `units: [String]`, `isDeleted: Bool`, and a plain
`section: Section?` back-reference (inverse declared on `Section`). `schedule: CardSchedule?` is
deliberately omitted — `CardSchedule` doesn't exist yet, per the task spec. `init` defaults
(`id = UUID()`, optionals `= nil`, `units = []`, `isDeleted = false`); `target`/`script` required.
Dropped the explicit `Sendable` conformance the spec's `enum Script` line suggested — SwiftFormat's
`redundantSendable` rule flags it as redundant for a non-public raw-value enum (implicitly Sendable
already), so `Script` is `String, Codable` only.

Edited `Sources/Models/Deck.swift`: added `@Relationship(deleteRule: .cascade, inverse:
\Note.section) var notes: [Note] = []` to `Section`, with a `notes: [Note] = []` init parameter.

Added `Tests/UnitTests/NoteModelTests.swift` (XCTest) against an in-memory
`ModelContainer(for: Deck.self, Section.self, Note.self, configurations:
ModelConfiguration(isStoredInMemoryOnly: true))`:
- `testNoteScalarFieldsRoundTrip` inserts a `Note(target: "ありがとう", pronunciation: "arigatou",
  english: "thank you", script: .hiragana, units: ["あ","り","が","と","う"])`, saves, refetches,
  and asserts every field round-trips intact (including `units` order and `isDeleted == false`).
- `testNoteAppendedToSectionRoundTripsRelationship` appends a `Note` to `section.notes`, saves,
  refetches the section, and asserts `notes.count == 1` and `note.section === section`.
- `testScriptRawValueMapping` asserts `Script(rawValue: "kanji") == .kanji` and
  `Script.mixed.rawValue == "mixed"`.

Checks run (all green): `./tools/ensure-dod-tools.sh`, `xcodegen generate`,
`./tools/bin/swiftformat Sources Tests --lint` (0/9 files need formatting),
`./tools/bin/swiftlint --strict` (0 violations), and
`xcodebuild test -project Kakitori.xcodeproj -scheme Kakitori -destination "name=Kakitori-Sim"
CODE_SIGNING_ALLOWED=NO` — 11 unit tests + 1 UI test, all passed (**TEST SUCCEEDED**).

Nothing remaining for this task.
