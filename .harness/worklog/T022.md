## 2026-07-17

Built the foundation types for the pure SRS scheduler module.

What I did:
- Created `Sources/Scheduler/Scheduler.swift` (Foundation-only) with:
  - `Grade` enum (again/hard/good/easy)
  - `ScheduleSnapshot` value struct mirroring `CardSchedule`'s scheduling fields
  - `SchedulePreview` struct
  - `Scheduler` protocol (`preview(for:now:)`, `apply(_:to:now:rng:)` with an injected
    `RandomNumberGenerator`)
  - `SRSConstants` namespace with all 20 constants from docs/03-srs-algorithm.md §3-4
  - `SplitMix64` seedable RNG
- Added `Tests/UnitTests/SchedulerTypesTests.swift` (XCTest): one assertion per
  `SRSConstants` value (20 constants) plus two `SplitMix64` tests (same seed -> identical
  first three `next()` values; different seed -> different first value).

Checks run:
- `./tools/ensure-dod-tools.sh` + `xcodegen generate`
- `./tools/bin/swiftformat Sources Tests --lint` — pass (auto-fixed two files, then
  re-verified clean)
- `./tools/bin/swiftlint --strict` — 0 violations
- `xcodebuild test -project Kakitori.xcodeproj -scheme Kakitori -destination
  'platform=iOS Simulator,name=Kakitori-Sim' -only-testing:KakitoriTests
  CODE_SIGNING_ALLOWED=NO` — all 88 tests pass (22 new)
- Verified `Sources/Scheduler/Scheduler.swift` imports only Foundation.

What remains: later tasks build the concrete `Scheduler` implementation, the SM-2 math,
interval fuzz, and `SchedulePreview.label` formatting (T025 per the spec).
