# T041 Worklog

## 2026-07-17

### Implementation

Created the audio service with protocol-backed implementation for deck MP3 playback and ja-JP speech fallback:

1. **AudioService.swift** (`Sources/Audio/AudioService.swift`):
   - `@MainActor protocol AudioPlaying` with `isAvailable`, `playDeckAudio()`, and `speakTarget()` methods
   - Extension on `AudioPlaying` with fallback-selection logic: `play()` chooses between audio playback or TTS based on filename presence
   - Static method `mediaFileURL()` to resolve deck media paths
   - `@MainActor final class AudioService: AudioPlaying` implementation:
     - `init(mediaBaseURL:)` resolves to `Application Support/Kakitori/Media` or temp directory
     - `playDeckAudio()` uses `AVAudioPlayer` with stored reference to prevent deallocation
     - `speakTarget()` uses `AVSpeechSynthesizer` with ja-JP voice
     - Missing files are silent no-ops, never crashes
     - `isAvailable` checks if ja-JP TTS voice exists

2. **FakeAudioPlayer.swift** (`Tests/UnitTests/FakeAudioPlayer.swift`):
   - Test helper implementing `AudioPlaying` protocol
   - Records calls to `playDeckAudio()` and `speakTarget()` for assertion
   - `calls` array tracks invocation order
   - No real AVFoundation instantiation

3. **AudioServiceTests.swift** (`Tests/UnitTests/AudioServiceTests.swift`):
   - URL resolution: verified exact UUID/filename path construction
   - Fallback selection: confirmed nil/empty filename triggers TTS, non-empty triggers playback
   - Call ordering: verified multiple `play()` calls record in sequence
   - No AVFoundation: confirmed fake player never constructs real audio objects

### Definition of Done

- ✅ Format: `./tools/bin/swiftformat Sources Tests --lint` — 0 issues
- ✅ Lint: `./tools/bin/swiftlint --strict` — 0 violations
- ✅ Test + Build:
  - Unit tests: 126 passed, 0 failures
  - UI tests: 1 passed, 0 failures
  - Exit code: 0
- ✅ Test coverage: 6 AudioServiceTests, all passing
  - `testMediaFileURLResolution`
  - `testPlayWithAudioFilenamePlaysAudio`
  - `testPlayWithNilAudioFilenameSpeaks`
  - `testPlayWithEmptyAudioFilenameSpeaks`
  - `testMultiplePlayCallsAreRecordedInOrder`
  - `testFakePlayerNeverCreatesAVAudioPlayer`

### Notes

- No force-unwraps in source; used guarded UUID initialization in test
- Protocol-based design allows easy swapping between real playback and test fakes
- Falls back to temporary directory if Application Support path cannot be resolved
- All audio instantiation remains in the live `AudioService` class; tests use `FakeAudioPlayer` only
