## Overview
The DeckSetupSheet header subtitle reads '<deck> · N cards due', but N (dueCount) is really the combined new + learning + due count (from TodayBannerView.calculateDueCards), so for a brand-new deck it misleadingly says e.g. '101 cards due' when the deck is 0 due / 101 new. Correct the wording so the label honestly describes the count it shows.

## Do
In Sources/Features/DeckSetup/DeckSetupSheet.swift the subtitle is `Text("\(enTitle) · \(dueCount) cards due")` (~line 103); dueCount is supplied by HomeView.dueCount(for:) which calls TodayBannerView.calculateDueCards(decks:now:).0 (HomeView.swift ~line 117-119), a total of new + learning + due cards. Fix the mismatch between the 'due' wording and the actual (new+learning+due) count. Prefer the smallest honest change: either relabel the count so it is accurate for a count that includes new cards (e.g. 'N cards' or 'N cards to study' rather than 'N cards due'), or, if trivial and clearly correct, split the shown figure into an accurate breakdown — but keep it a wording/label fix, not a scheduler change. Do NOT modify the SM-2 scheduler or the calculateDueCards computation itself. Keep swiftformat/swiftlint clean; the UI is bilingual so this is English copy only unless a matching pattern already exists. Do not hand-edit the generated .xcodeproj.

## Done when
The DeckSetupSheet subtitle no longer claims 'N cards due' over a count that includes new/learning cards — the label accurately describes the figure it shows (verified by a screenshot on Kakitori-Sim-Phone and Kakitori-Sim showing the corrected wording for a fresh deck that is 0 due / all-new). The DoD gate (xcodegen generate; swiftformat --lint; swiftlint --strict; xcodebuild test on Kakitori-Sim) passes.
