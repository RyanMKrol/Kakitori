## Overview
Re-attempt of the ORIGINAL T023 (SM2Scheduler learning/relearning grade transitions). The first attempt's CODE was never the problem: the loop's scope-creep gate blocked it because the builder committed the `TestResults.xcresult/` bundle that `xcodebuild test` drops into the repo root (it is NOT git-ignored), sweeping hundreds of out-of-scope paths into the commit via an over-broad `git add`. This re-attempt keeps the identical scheduler spec and adds two guardrails: git-ignore that artifact, and stage ONLY the intended files.

## Do
Create `Sources/Scheduler/SM2Scheduler.swift` (pure Foundation only — no SwiftData/SwiftUI imports, no `Date()`/`.now`, no system RNG). T022 already provides `Grade`, `ScheduleSnapshot`, `SchedulePreview`, `SRSConstants`, `SplitMix64` (all in `Sources/Scheduler/Scheduler.swift`); `CardState` is in `Sources/Models/CardSchedule.swift`. Define `struct SM2Scheduler` WITHOUT `: Scheduler` conformance (T024 adds it) with:

```swift
init()
func apply(_ grade: Grade, to card: ScheduleSnapshot, now: Date,
           rng: inout some RandomNumberGenerator) -> ScheduleSnapshot
```

`rng` is unused here (fuzz arrives in T024) but the signature is fixed now. Behavior, using `SRSConstants` (learning steps = [60s, 600s]; relearning single 600s step; graduating interval 1 day; easy graduating interval 4 days; day = 86_400s), matching docs/03-srs-algorithm.md §'Learning / relearning cards':
- `.new` — treat exactly as `.learning` at stepIndex 0.
- `.learning` (steps [60,600]s): again → stepIndex 0, `.learning`, dueAt = now+60. hard → repeat current step (dueAt = now + learningStepsSeconds[stepIndex], no advance). good → advance; if stepIndex was last (1) graduate: `.review`, intervalDays 1, stepIndex 0, dueAt = now + 86_400; else stepIndex += 1, dueAt = now + learningStepsSeconds[newIndex] (600). easy → graduate immediately: `.review`, intervalDays 4, stepIndex 0, dueAt = now + 345_600. easeFactor/lapses/intervalDays unchanged by learning grades.
- `.relearning` (single 600s step; intervalDays holds pre-lapse interval I): again/hard → stay `.relearning`, stepIndex 0, dueAt = now+600. good → re-graduate: `.review`, intervalDays = max(1, (I × 0.5).rounded()), stepIndex 0, dueAt = now + intervalDays×86_400. easy → re-graduate at intervalDays = max(4, max(1, (I × 0.5).rounded())) so Easy is never worse than Good.
- `.review` — placeholder until T024: return the input snapshot unchanged (no fatalError).
- NO fuzz anywhere; all dueAt offsets exact. Bounds-safe indexing (clamp stepIndex into the steps array); no force-unwraps.

GUARDRAILS (the reason the first attempt was blocked):
1. Add a line `TestResults.xcresult/` (and, if not already present, `*.xcresult`) to `.gitignore`, so the `xcodebuild test` result bundle that the local DoD drops in the repo root is never a candidate for staging.
2. When committing, stage ONLY the files you actually created/edited — `git add Sources/Scheduler/SM2Scheduler.swift Tests/... .gitignore`. NEVER `git add -A` / `git add .`, which would re-capture the build artifact and re-trip the scope-creep gate exactly as before.

## Done when
`Tests/UnitTests/SM2LearningTests.swift` (XCTest) passes against an injected fixed `now = Date(timeIntervalSince1970: 1_000_000)` and `var rng = SplitMix64(seed: 1)`, asserting exact `state` / `stepIndex` / `intervalDays` AND `dueAt == now + offset` for every case (offsets from docs/03-srs-algorithm.md steps 60s/600s, day=86_400s):
- learning step 0 (EF 2.5): again → step 0, +60s; hard → step 0, +60s; good → step 1, +600s; easy → review, interval 4, +345_600s.
- learning step 1: again → step 0, +60s; hard → step 1, +600s; good → review, interval 1, +86_400s; easy → review, interval 4, +345_600s.
- relearning intervalDays=10, EF 2.3, lapses 1: again → relearning, +600s; hard → relearning, +600s; good → review, interval 5, +432_000s, lapses still 1; easy → review, interval 5, +432_000s.
- relearning intervalDays=2: good → review, interval 1, +86_400s; easy → review, interval 4, +345_600s.
- new (state `.new`, step 0): good → `.learning`, step 1, +600s; easy → `.review`, interval 4, +345_600s.
- `easeFactor` asserted UNCHANGED in every case above.
The full local DoD (swiftformat --lint, swiftlint --strict, xcodebuild test) passes, AND the committed diff contains ONLY the scheduler source, the test file, and the `.gitignore` change — no `TestResults.xcresult/` paths (which is what proves the scope-creep root cause is fixed).
