## Overview
Idea #3. On the session-complete Summary screen, replace the confusing pair of buttons (both currently just return Home) with two clear buttons: a Free Study button on TOP that immediately launches a Free Study session for the SAME deck just finished, and 'Back home'/'Back to decks' on the BOTTOM. Remove the old 'Study another' button and its onStudyAnother path entirely. Use KakitoriTheme.freeStudyLabel for the Free Study button text so it matches the deck-setup button and the running-session badge.

## Do
In Sources/Features/Summary/SummaryView.swift: replace the onStudyAnother closure (line 13) with onStartFreeStudy: () -> Void. In buttonsView (~lines 106-134), reorder so the Free Study button is FIRST/top: a button whose label is Text("Start " + KakitoriTheme.freeStudyLabel) (freeStudyLabel = "Free Study"), styled with the blue KakitoriTheme.freeStudyAccent background to match the deck-setup entry, accessibilityIdentifier "start-free-study", calling onStartFreeStudy; and the existing "Back home"/"Back to decks" button SECOND/bottom (keep accessibilityIdentifier "back-to-decks", keep its isCompact copy). Remove the old "study-another" button entirely.

In Sources/Features/Home/HomeView.swift: in the SummaryView(...) construction (~lines 99-116), replace the onStudyAnother: argument with onStartFreeStudy: { if let deck = activeSessionDeck { activeSession = nil; startFreeStudySession(deck: deck, mode: <the just-finished session's mode>) } }. The just-finished session's mode is available from the finished SessionViewModel (its `mode` property, SessionViewModel line 31) — pass that so "keep going" continues in the SAME writing mode the user was using; if for any reason it isn't reachable, default to .trace. This uses the startFreeStudySession(deck:mode:) signature introduced in fsep-b. Keep onBackToDecks returning Home. Ensure no dangling onStudyAnother reference remains anywhere. This "keep going" Free Study button must work after BOTH a normal SRS session AND a Free Study session (the owner confirmed a Free Study session's summary should also offer Free Study again).

## Done when
The Summary screen shows exactly two buttons: a Free Study button on TOP (accessibilityIdentifier "start-free-study", label "Start Free Study" from KakitoriTheme.freeStudyLabel, blue KakitoriTheme.freeStudyAccent) and "Back home"/"Back to decks" on the BOTTOM (accessibilityIdentifier "back-to-decks"); the old "Study another" button (identifier "study-another", onStudyAnother) is gone with no dangling references. Tapping Free Study immediately launches an endless Free Study session for the SAME deck just finished, in the SAME writing mode the finished session used (via startFreeStudySession(deck:mode:)); tapping Back home returns to the Home deck list. This works after both a normal SRS session and a Free Study session (a Free Study summary also re-offers Free Study). Verified visually on BOTH devices: iPad (Kakitori-Sim) and iPhone/compact (Kakitori-Sim-Phone) — button order (Free Study top, Back home bottom) and compact vs regular copy both correct, neither layout broken. DoD green: xcodegen generate, swiftformat --lint, swiftlint --strict, xcodebuild test on Kakitori-Sim (never a bare simulator model name).
