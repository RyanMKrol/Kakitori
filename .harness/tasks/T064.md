## Overview
In the deck-setup sheet's practice-mode picker, the 'Mixed' mode's subtitle reads 'Rotate through all four modes' (Sources/Features/DeckSetup/DeckSetupSheet.swift, PracticeMode.description case .mixed, ~line 36; rendered at ~line 156 via Text(mode.description)). This is factually wrong: Mixed is itself a mode, so it rotates through the OTHER available modes. For the kana-only decks shipping today the other modes are Trace, Listen & Write, Recall = three. This unit corrects the wording so it no longer claims 'four'. The number of non-Mixed modes is deck-dependent (ModeAvailability.deckModes adds Translate for kanji/mixed-script decks -> four non-Mixed), and the sheet already receives the deck's availableModes (DeckSetupSheet.availableModes), so a dynamic count from that array is feasible without touching the Scheduler layer. NOTE: the exact wording -- static 'three', a dynamic count derived from availableModes, or a count-free 'the other modes' -- is a PLANNING question relayed to the owner (.harness/.pending-questions/conv-mixed-copy.json). Implement the owner's chosen approach; if unanswered at build time, prefer the dynamic count from availableModes (reads 'three' for today's kana decks, self-corrects for a future Translate deck) as it satisfies the literal ask without going stale.

## Do
Correct the 'Mixed' practice-mode subtitle in Sources/Features/DeckSetup/DeckSetupSheet.swift so it no longer says 'four modes'. The static PracticeMode.description case .mixed currently returns "Rotate through all four modes". Apply the owner's chosen wording from .harness/.pending-questions/conv-mixed-copy.json; if unanswered, derive the count from the sheet's availableModes (count of available modes excluding .mixed) so the Mixed row's subtitle reflects the actual number of non-Mixed modes (three for the current kana-only bundled decks). If you make the subtitle dynamic, drive it from availableModes already held by DeckSetupSheet -- do NOT reach into ModeAvailability (Sources/Scheduler) or add a Scheduler dependency; keep the change within Sources/Features/DeckSetup/**. If PracticeMode.description remains a static per-case string, keep the other cases' text unchanged. Update the #Preview if needed so it still compiles. This is a copy/style change with no logic behaviour beyond the displayed string.

## Done when
The 'Mixed' practice-mode row's subtitle in DeckSetupSheet no longer contains 'four modes'; for the current kana-only bundled decks (availableModes = Trace, Listen, Recall, Mixed) it reads with the correct count of non-Mixed modes (three), matching the owner's chosen wording. The deck-setup sheet compiles and renders the corrected Mixed-row copy on BOTH iPhone (compact) and iPad layouts -- the same DeckSetupSheet copy is used on both, so verify it appears correctly on each. No other practice-mode subtitles change. The full Definition of Done passes: xcodegen generate, swiftformat --lint, swiftlint --strict, and xcodebuild test all green.
