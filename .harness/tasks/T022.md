# T022 — Scheduler protocol, Grade enum, ScheduleSnapshot, SRS constants

## Overview

Create the foundation types of the pure SRS module: the `Grade` enum, the `ScheduleSnapshot` value
struct, the `Scheduler` protocol, every SRS constant from docs/03-srs-algorithm.md §3 as a named
static, and a seedable RNG for later fuzz work.

## Do

Create `Sources/Scheduler/Scheduler.swift`. **Module rule (applies to every file in
`Sources/Scheduler/`, this task and all later ones):** pure Foundation only — `import Foundation`
and nothing else. `import SwiftData` / `import SwiftUI` here is a build-failing violation. So is any
`Date()` / `.now` (time is always a `now: Date` parameter) or use of the system RNG.

Established facts (already built; do not recreate): `CardState` enum
(`case new, learning, review, relearning`) lives in `Sources/Models/CardSchedule.swift` — it is a
plain Swift enum in the same app target, usable here with no extra import.

Define, in this file:

- `enum Grade: String, CaseIterable, Hashable, Sendable { case again, hard, good, easy }`
- `struct ScheduleSnapshot: Equatable, Sendable` — a plain value mirror of `CardSchedule`'s
  scheduling fields, with a memberwise init: `state: CardState`, `stepIndex: Int`,
  `easeFactor: Double`, `intervalDays: Double`, `dueAt: Date?`, `lapses: Int`. Callers (the session
  view model, later) map `CardSchedule` ↔ snapshot field-by-field; no SwiftData types appear here.
- `struct SchedulePreview: Equatable, Sendable { let dueAt: Date; let intervalDays: Double; let label: String }`
  (`label` is the compact button string, e.g. `"<1m"`, `"10m"`, `"4d"` — produced in T025).
- `protocol Scheduler` with exactly:
  `func preview(for card: ScheduleSnapshot, now: Date) -> [Grade: SchedulePreview]` and
  `func apply(_ grade: Grade, to card: ScheduleSnapshot, now: Date, rng: inout some RandomNumberGenerator) -> ScheduleSnapshot`.
  (docs/05-architecture.md §4, extended with the required injected-RNG parameter for interval fuzz.)
- `enum SRSConstants` (caseless namespace) — every value cited from docs/03-srs-algorithm.md §3–§4:
  `learningStepsSeconds: [TimeInterval] = [60, 600]` (steps 1 min, 10 min);
  `graduatingIntervalDays: Double = 1`; `easyGraduatingIntervalDays: Double = 4`;
  `relearningStepSeconds: TimeInterval = 600` (10 min); `initialEase: Double = 2.5`;
  `minimumEase: Double = 1.3`; `againEaseDelta: Double = -0.20`; `hardEaseDelta: Double = -0.15`;
  `easyEaseDelta: Double = 0.15`; `hardIntervalMultiplier: Double = 1.2`; `easyBonus: Double = 1.3`;
  `maximumIntervalDays: Double = 365`; `minimumReviewIntervalDays: Double = 1`;
  `lapseIntervalMultiplier: Double = 0.5`; `fuzzFraction: Double = 0.05` (±5%);
  `fuzzMinimumIntervalDays: Double = 3`; `defaultNewPerDay: Int = 10`;
  `defaultMaxReviewsPerDay: Int = 100`; `dayRolloverHour: Int = 4` (4 AM);
  `secondsPerDay: TimeInterval = 86_400`.
- `struct SplitMix64: RandomNumberGenerator, Sendable` — seedable RNG for tests and fuzz:
  `init(seed: UInt64)` stores `state`; `mutating func next() -> UInt64` does
  `state &+= 0x9E3779B97F4A7C15; var z = state; z = (z ^ (z >> 30)) &* 0xBF58476D1CE4E5B9;
  z = (z ^ (z >> 27)) &* 0x94D049BB133111EB; return z ^ (z >> 31)`.

## Done when

- `Tests/UnitTests/SchedulerTypesTests.swift` (XCTest) pins every `SRSConstants` value to exactly
  the numbers above (one assertion per constant — e.g.
  `XCTAssertEqual(SRSConstants.learningStepsSeconds, [60, 600])`,
  `XCTAssertEqual(SRSConstants.minimumEase, 1.3)`, … all 20 constants).
- Same file: two `SplitMix64(seed: 42)` instances produce identical first three `next()` values,
  and a different seed (43) produces a different first value.
- No file in `Sources/Scheduler/` imports anything except Foundation.
