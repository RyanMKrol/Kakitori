## Overview
Re-attempt of T002, which was BLOCKED not on the actual work but on tooling hygiene: the DoD test command runs `xcodebuild test ... -resultBundlePath TestResults.xcresult`, which drops a `TestResults.xcresult` bundle at the repo root. That bundle is NOT gitignored, so when the builder staged everything (`git add -A`) it committed the bundle, and the loop's scope-creep gate (which diffs committed files against the task's `scope`) failed on the first out-of-scope path with no escalation. This re-attempt does the identical Theme.swift work AND closes the artifact leak by gitignoring the result bundle.

## Do
Two parts.

PART 1 — prevent the artifact leak (the reason T002 died). Add `TestResults.xcresult/` (and, for safety, `*.xcresult`) to the repo-root `.gitignore` in the general Xcode section (NOT inside the harness-managed marker block, which is auto-maintained). The DoD test step writes `TestResults.xcresult` into the working tree; it must never be committed. Also DO NOT run `git add -A` / `git add .` — stage only the files this task actually creates (`Sources/Support/Theme.swift`, any test file, `.gitignore`), so a stray build artifact can never sneak into the commit.

PART 2 — the original component work, unchanged. Create `Sources/Support/Theme.swift` containing one caseless public enum `KakitoriTheme` (namespace, no instances) with these static tokens (values quoted from docs/06-ui-design.md §1):
- Palette (SwiftUI `Color`, via a private `Color(hex:)` helper or explicit `Color(red:green:blue:)` component math — NO force-unwraps, no `try!`): `paper` = `#FAF8F4`; `ink` = `#1A1A1A`; `inkFaint` = `ink` at 12% opacity; `accent` = vermillion `#C43B2E`; `boxLine` = `ink` at 8% opacity.
- Chip styles: `chipNewBackground` = `ink` @8%, `chipNewForeground` = `ink`; `chipLearnBackground` = `accent` @12%, `chipLearnForeground` = `accent`; `chipDueBackground` = `ink`, `chipDueForeground` = `paper`.
- Shape: `radiusLarge: CGFloat = 20`, `radiusMedium: CGFloat = 16`.
- Typography: `static func japaneseDisplayFont(size: CGFloat, bold: Bool = false) -> Font` returning `Font.custom("HiraMinProN-W6", size: size)` when bold else `Font.custom("HiraMinProN-W3", size: size)`; `static func smallCapsLabel(size: CGFloat = 12) -> Font` returning `.system(size: size, weight: .semibold)`.
Swift 6 strict concurrency: all tokens are `let`/computed statics on the caseless enum (inherently Sendable). SwiftUI only — no UIKit import, no `Date()`.

## Done when
`Sources/Support/Theme.swift` compiles with exactly the tokens above; the five palette colors match `#FAF8F4`, `#1A1A1A`, 12%-ink, `#C43B2E`, 8%-ink; `japaneseDisplayFont(size:bold:)` uses HiraMinProN-W3/W6; no force-unwraps or `try!` in the file. `.gitignore` contains an entry ignoring `TestResults.xcresult/` (and/or `*.xcresult`), so `git status` after a DoD test run shows the result bundle as ignored — it never appears in the commit. The commit's `git diff --name-only origin/main..HEAD` lists ONLY `Sources/Support/Theme.swift`, `.gitignore`, any added test file, and worklog — no `TestResults.xcresult/*` paths. The full DoD (format, lint, test+build) passes.
