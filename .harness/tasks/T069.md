## Overview
On iPhone (compact size class) the deck practice-mode sheet (DeckSetupSheet) is presented by HomeView with a .medium presentation detent that is too short for its content, so the pinned full-width 'Start writing' CTA is pushed off the bottom of the screen and only a thin sliver is visible. The sheet must present so its entire content — including the CTA — is reachable on iPhone, while iPad's existing .large presentation stays exactly as it is.

## Do
In Sources/Features/Home/HomeView.swift the .sheet(isPresented: setupSheetBinding) block presents DeckSetupSheet with `.presentationDetents(horizontalSizeClass == .compact ? [.medium] : [.large])` (around line 55). On compact/iPhone, .medium cannot fit the sheet content: the ink header band, the 'PRACTICE MODE' caption, four ~65pt mode rows (Trace / Listen & Write / Recall / Mixed), and the pinned full-width 'Start writing' CTA (accessibilityIdentifier 'start-writing', DeckSetupSheet.swift ~line 188-205). Change the presentation so the whole sheet including the CTA is reachable on iPhone. Choose the cleanest mechanism: use .large on compact too, use a content-fitted / .fraction / custom presentation detent sized to the content, or make the sheet body scrollable while keeping the CTA pinned and always reachable. Keep the DeckSetupSheet layout structure (headerBand, mode list, pinned startButton) intact; do not alter iPad behaviour. Do NOT change copy or counts in this task (the 'N cards due' wording is a separate unit). Run swiftformat/swiftlint clean; the .xcodeproj is generated by xcodegen — do not hand-edit it.

## Done when
On iPhone (Kakitori-Sim-Phone) DeckSetupSheet presents with its full content visible and usable — header, PRACTICE MODE caption, all four mode rows, and the full-width 'Start writing' CTA (accessibilityIdentifier 'start-writing') fully on-screen and tappable, no longer clipped to a sliver — verified by screenshot (./build_run.sh Kakitori-Sim-Phone). AND on iPad (Kakitori-Sim) the sheet's existing .large presentation is visually unchanged — verified by screenshot. The DoD gate (xcodegen generate; swiftformat --lint; swiftlint --strict; xcodebuild test on Kakitori-Sim) passes.
