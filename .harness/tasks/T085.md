## Overview
Idea #7: fix the label-vs-behaviour mismatch on the Summary screen. The secondary button reads 'Study another script'/'Study another deck' (Sources/Features/Summary/SummaryView.swift, accessibilityIdentifier 'study-another') but reopens DeckSetupSheet for the SAME deck just finished (via HomeView onStudyAnother + the fullScreenCover onDismiss). DECIDED by the owner: fix the BEHAVIOUR to match the label — 'Study another' dismisses the session and lands the user on the HOME DECK LIST so they can pick a DIFFERENT script — and stays meaningfully distinct from 'Back home'. Pure-UI navigation change; visual verification on both devices.

## Do
1. Change the 'Study another' wiring so it no longer reopens DeckSetupSheet for the just-finished deck. In Sources/Features/Home/HomeView.swift: remove the deckPendingRestudy=activeSessionDeck path in onStudyAnother and the onDismiss re-open-same-deck logic that reopens setupDeck for that deck. Instead route onStudyAnother to DISMISS the session (nil out activeSession/activeSessionDeck) and land on Home with the deck list visible so the user chooses a different deck (DECIDED: Home deck list, NOT a dedicated picker sheet). Do NOT auto-launch a session for any deck — the user must choose.
2. Keep it meaningfully distinct from onBackToDecks: 'Back home' also returns to Home, so ensure 'Study another' clearly reads as 'pick another script to study' (keep/adjust the button label so the two are not confusing) even though both land on Home.
3. Remove the now-unused deckPendingRestudy @State if nothing else uses it.
4. Keep the two Summary buttons' accessibilityIdentifiers stable ('back-to-decks', 'study-another'). Sanity-check the compact 'script' vs regular 'deck' wording so it stays consistent with landing on the Home deck list.
5. Dual-device: verify on BOTH iPhone (Kakitori-Sim-Phone) and iPad (Kakitori-Sim).

## Done when
Tapping 'Study another' (accessibilityIdentifier 'study-another') on the Summary screen no longer reopens DeckSetupSheet for the deck just completed; it dismisses the session and lands the user on the Home deck list where they can choose a DIFFERENT deck/script. 'Back home'/'Back to decks' ('back-to-decks') remains the plain leave action and the two buttons are meaningfully distinct (label makes 'pick another' clear). The obsolete deckPendingRestudy same-deck re-open path is removed. The compact 'script' / regular 'deck' wording is consistent with landing on the Home deck list. Verified on iPhone (Kakitori-Sim-Phone) and iPad (Kakitori-Sim).
