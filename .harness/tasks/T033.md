# T033 — Action row: Show answer + four grade buttons with interval previews

## Overview

Build the action row under the canvas: a full-width "Show answer" button that springs into the four
grade buttons (Again/Hard/Good/Easy) on reveal, each showing its SM-2 interval preview, with grading
advancing immediately.

## Do

- Create `Sources/Features/Session/ActionRowView.swift` taking the `SessionViewModel`; replace the
  `actionRow` placeholder in `Sources/Features/Session/SessionView.swift` with it.
- **Before reveal**: one full-width button `Show answer` — ink-solid fill, paper text, ~14pt corner
  radius, ≥44pt tall, identifier `"show-answer"` → calls `viewModel.showAnswer()`
  (docs/02-product-spec.md §1.3).
- **After reveal** (view model's `revealed` flag): four equal-width buttons in a row —
  labels `Again` / `Hard` / `Good` / `Easy`, identifiers `"grade-again"` / `"grade-hard"` /
  `"grade-good"` / `"grade-easy"`. Each shows its interval preview UNDER the label, taken from the
  per-grade preview strings `SessionViewModel` exposes (e.g. "<1m", "10m", "1d", "4d") — layout per
  docs/06-ui-design.md §2.3 ("interval previews under each label"). NOTE: the doc's mock literally
  shows "Hard 6m", but no docs/03 SM-2 constant can produce 6m (Hard repeats the current 1m/10m
  learning step) — that mock value is a prototype artifact. NEVER hardcode preview strings; always
  render whatever `SessionViewModel`'s per-grade previews return.
- **Styling** per docs/06 §2.3: "Again tinted red, Easy tinted ink-solid, middle two neutral" —
  Again = accent fill, Hard/Good = white fill + hairline border, Easy = ink fill with paper text.
  (The HTML prototype paints Good ink-solid instead — the doc wins; note it in the worklog.)
- Tapping a grade calls `viewModel.grade(_:)` — the session advances immediately and the row falls
  back to "Show answer" for the next card (docs/02 §1.3).
- Reveal transition: the grade row springs up (spring animation, ≤250ms perceived — docs/06 §3:
  "grade row springs up").
- Render check: `Tests/UnitTests/ActionRowRenderTests.swift`, same recipe as
  `Tests/UnitTests/SessionScaffoldRenderTests.swift` (in-memory container, fixed clock,
  `ImageRenderer` at 1194×834, `#filePath`-derived output): write `screenshots/T033-show-answer.png`
  pre-reveal and `screenshots/T033-grades.png` after `showAnswer()`.

## Done when

- `screenshots/T033-show-answer.png` shows the single full-width ink "Show answer" button below the
  canvas area.
- `screenshots/T033-grades.png` shows four grade buttons with interval previews under each label,
  Again red-tinted and Easy ink-solid.
- `./build_run.sh Kakitori-Sim -seedDemoData YES` still builds and launches clean.
